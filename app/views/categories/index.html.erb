<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1>📂 Categorías</h1>
    <%= link_to '➕ Nueva Categoría', new_category_path, class: 'btn btn-success' %>
  </div>

  <% if notice %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <%= notice %>
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
  <% end %>

  <% if @categories.any? %>
    <div class="table-responsive">
      <table class="table table-hover">
        <thead class="table-dark">
          <tr>
            <th>Nombre</th>
            <th class="text-end">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% @categories.each do |category| %>
            <tr>
              <td><%= category.name %></td>
              <td class="text-end">
                <%= link_to 'Ver', category_path(category), class: 'btn btn-sm btn-info' %>
                <%= link_to 'Editar', edit_category_path(category), class: 'btn btn-sm btn-warning' %>
                <%= link_to 'Eliminar', category, data: { turbo_method: :delete, turbo_confirm: '¿Eliminar categoría?' }, class: 'btn btn-sm btn-danger' %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="alert alert-info">No tienes categorías. Crea la primera.</div>
  <% end %>
</div>
